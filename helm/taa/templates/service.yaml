apiVersion: v1
kind: Service
metadata:
  name: {{ include "taa.fullname" . }}
  labels:
    {{- include "taa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taa.selectorLabels" . | nindent 4 }}
