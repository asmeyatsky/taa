apiVersion: v1
kind: Secret
metadata:
  name: {{ include "taa.fullname" . }}
  labels:
    {{- include "taa.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
