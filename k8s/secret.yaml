apiVersion: v1
kind: Secret
metadata:
  name: taa-secrets
  namespace: taa
  labels:
    app.kubernetes.io/name: taa
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # REQUIRED: Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
  # DO NOT commit real values — use sealed-secrets, external-secrets, or vault
  TAA_SECRET_KEY: ""
  TAA_DATABASE_URL: ""
  TAA_ENV: "production"
